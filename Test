Sub CopyPivotWithSlicerToPPT()
    Dim pptApp As Object
    Dim pptPres As Object
    Dim pptSlide As Object
    Dim ws As Worksheet
    Dim pt As PivotTable
    Dim sc As SlicerCache
    Dim si As SlicerItem
    Dim i As Integer

    ' Set your worksheet containing the PivotTable
    Set ws = ThisWorkbook.Sheets("Sheet1") ' Change "Sheet1" to your sheet name

    ' Set your PivotTable
    Set pt = ws.PivotTables("PivotTable1") ' Change "PivotTable1" to your PivotTable name

    ' Set your SlicerCache
    Set sc = ThisWorkbook.SlicerCaches("Slicer_BusinessArea") ' Change "Slicer_BusinessArea" to your SlicerCache name

    ' Initialize PowerPoint
    On Error Resume Next
    Set pptApp = GetObject(, "PowerPoint.Application")
    If pptApp Is Nothing Then
        Set pptApp = CreateObject("PowerPoint.Application")
    End If
    On Error GoTo 0

    pptApp.Visible = True

    ' Create a new presentation if none is open
    If pptApp.Presentations.Count = 0 Then
        Set pptPres = pptApp.Presentations.Add
    Else
        Set pptPres = pptApp.ActivePresentation
    End If

    ' Loop through each item in the slicer cache
    i = 1
    For Each si In sc.SlicerItems
        ' Clear all filters
        sc.ClearManualFilter

        ' Select only the current slicer item
        si.Selected = True

        ' Refresh the PivotTable
        pt.RefreshTable

        ' Create a new slide
        Set pptSlide = pptPres.Slides.Add(i, 12) ' 12 = ppLayoutText
        pptSlide.Shapes(1).TextFrame.TextRange.Text = "Business Area: " & si.Name

        ' Copy the PivotTable
        pt.TableRange2.Copy

        ' Paste PivotTable into PowerPoint
        pptSlide.Shapes.PasteSpecial DataType:=2 ' 2 = ppPasteEnhancedMetafile
        pptSlide.Shapes(2).Left = 100
        pptSlide.Shapes(2).Top = 100

        i = i + 1
    Next si

    ' Clear slicer filter to show all items
    sc.ClearManualFilter

    ' Clean up
    Set pptSlide = Nothing
    Set pptPres = Nothing
    Set pptApp = Nothing
    Set ws = Nothing
    Set pt = Nothing
    Set sc = Nothing
End Sub
