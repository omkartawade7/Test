SELECT 
    decimal_time,
    
    -- Round time to the nearest half-hour
    floor(decimal_time / 60) * 60 + 
    CASE 
        WHEN (decimal_time % 60) < 15 THEN 0
        WHEN (decimal_time % 60) < 45 THEN 30
        ELSE 0 
    END AS rounded_time
FROM 
    agent_schedule;
